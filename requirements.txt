# Python packages. On Ubuntu these usually come from apt already:
#   sudo apt install python3-requests python3-pil python3-tk python3-gi python3-evdev
requests>=2.31.0
Pillow>=10.0.0
evdev>=1.6.0

# Not installable from PyPI in a useful way: PyGObject bindings for the XDG
# desktop portal come from the system package python3-gi.
